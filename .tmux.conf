# http://d.hatena.ne.jp/tmatsuu/20090709/1247150771
# status
# set -g status-fg cyan
# set -g status-bg black
#  #
#  set -g status-left-length 30
#  set -g status-left '#[fg=black,bg=yellow]#H#[fg=black]:#[fg=black][#S#[fg=black]][#[default]'
#  set -g status-right '#[fg=black,bg=cyan,bold] [%Y-%m-%d(%a) %H:%M]#[default]'
#  set -g status-right '#[fg=black,bg=cyan,bold] [%Y-%m-%d(%a) %H:%M]#[default] #[fg=blue,bold][CPU=#(~/bin/getCpuUsage.sh) MEM=#(~/bin/getMemUsage.sh)]#[default]'
#   
#   # window-status-current
#   setw -g window-status-current-fg black
#   setw -g window-status-current-bg cyan
#   setw -g window-status-current-attr bold#,underscore
#    
#    # pane-active-border
#    set -g pane-active-border-fg black
#    set -g pane-active-border-bg cyan

#set-option -g mouse-select-pane on
#set-window-option -g mode-keys vi
#set-window-option -g utf8 on

#bind s split-window -v
#bind v split-window -h
#bind h select-pane -L
#bind j select-pane -D
#bind k select-pane -U
#bind l select-pane -R

#bind i display-panes
#bind Space choose-window

# ショートカット
 # set-option            set
 # set-window-option     setw

 # 真真真真 臣trl-z
 set-option -g prefix C-z

 # 真真真真|?
 setw -g mode-keys vi

 ## 真真真真真真真真真
 #set-option -g mouse-select-pane
 #set-option -g mouse-resize-pane

 # 真真真真|?
 # Ctrl-z-e 刃N臣trl-z-E 刃FF
 bind e setw synchronize-panes on
 bind E setw synchronize-panes off

 #color
 set -g status-fg white
 set -g status-bg colour18
 setw -g window-status-current-fg red
 setw -g window-status-current-bg colour18
 setw -g window-status-current-attr bold

 # 真真|?
 # Ctrl-z-v真真真Ctrl-z-h真真|?
 bind v split-window -v
 bind h split-window -h


 # ペインの移動2
 # Shift + 上下左右でペインを移動できるようにする。(＜Prefix＞不要)
 # 行頭に"bind -n"をつけるとPrefixが不要になる。
 bind -n S-left select-pane -L
 bind -n S-down select-pane -D
 bind -n S-up select-pane -U
 bind -n S-right select-pane -R
